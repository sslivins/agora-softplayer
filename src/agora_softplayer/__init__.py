"""Windows-native softplayer for the Agora digital-signage CMS.

Reuses cms_client + shell server from the vendored `agora/` submodule and
adds Windows-specific browser launching + Linux-shim layers so the whole
thing runs as a normal Windows process.
"""
__version__ = "0.1.1"
